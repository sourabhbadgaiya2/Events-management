import { EventType } from "../../../../interface";

const TicketsSelection = ({ eventData }: { eventData: EventType }) => {
  return <div>TicketsSelections</div>;
};

export default TicketsSelection;
